// globals.js

// References to globally needed instances or data
export const globals = {
    gameManager: null,
    pixiApp: null,
    // interactionManager is typically accessed via renderer.plugins.interaction
};

// No getResource/getTexture helpers needed, use PIXI.loader.resources directly.